/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["https://via.placeholder.com" ,"via.placeholder.com","https://via.placeholder.com"], 
       
      },
};

export default nextConfig;
